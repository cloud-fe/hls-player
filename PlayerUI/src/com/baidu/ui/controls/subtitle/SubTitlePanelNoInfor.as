package com.baidu.ui.controls.subtitle  {
	
	import flash.display.MovieClip;
	import flash.events.IEventDispatcher;
	import flash.events.MouseEvent;
	import flash.events.Event;
	
	
	public class SubTitlePanelNoInfor extends MovieClip implements IEventDispatcher {
		
		public function SubTitlePanelNoInfor() {
			//btnSearch.addEventListener(MouseEvent.CLICK,onMouseClickEvent);
			
			//btnConfig.addEventListener(MouseEvent.CLICK,onMouseClickEvent);
		}
		private function onMouseClickEvent(e:MouseEvent):void{
			//switch(e.target){
				//case btnSearch:
					//dispatchEvent(new Event(SubTitlePanel.SEARCH));
					//break;
				//case btnConfig:
					//dispatchEvent(new Event(SubTitlePanel.CONFIG));
					//break;
			//}
			//e.stopImmediatePropagation();
		}
	}
	
}
